# 다크패턴 진단 센터

사용자 경험을 해치는 악의적인 디자인 패턴(다크패턴)을 진단하고 예방하는 웹사이트입니다.

## 🎯 주요 기능

- **가이드라인 페이지**: 6가지 다크패턴 유형별 설명 및 예방법 제공
- **진단 테스트**: 5개 질문을 통한 인터랙티브 다크패턴 진단
- **Firebase 통합**: 사용자 응답을 실시간 데이터베이스에 저장
- **결과 분석**: 위험도 평가 및 개선 제안

## 📋 다루는 다크패턴

1. **숨겨진 옵션** - 탈퇴/해지 버튼이 숨겨짐
2. **오도** - 혼동스러운 언어나 부정확한 정보
3. **비대칭적 선택지** - 선택 해제가 선택보다 어려움
4. **긴박감 조성** - 거짓 제한 시간 또는 재고 메시지
5. **사전 체크** - 기본값이 사용자에게 불리하게 설정
6. **가짜 사회적 증거** - 조작된 리뷰 및 평점

## 🚀 시작하기

### 1. 프로젝트 설치

```bash
npm install
```

### 2. Firebase 설정

1. [Firebase Console](https://console.firebase.google.com)에서 프로젝트 생성
2. Realtime Database 활성화 (서울 지역 권장)
3. 데이터베이스 규칙을 다음과 같이 설정:

```json
{
  "rules": {
    "diagnostics": {
      ".write": true,
      ".read": true
    }
  }
}
```

3. 프로젝트 설정 > 서비스 계정 > 데이터베이스 URL 복사

### 3. 환경 변수 설정

`.env.local` 파일을 수정하여 Firebase 설정 입력:

```env
NEXT_PUBLIC_FIREBASE_API_KEY=YOUR_API_KEY
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=YOUR_AUTH_DOMAIN
NEXT_PUBLIC_FIREBASE_PROJECT_ID=YOUR_PROJECT_ID
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=YOUR_STORAGE_BUCKET
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=YOUR_MESSAGING_SENDER_ID
NEXT_PUBLIC_FIREBASE_APP_ID=YOUR_APP_ID
NEXT_PUBLIC_FIREBASE_DATABASE_URL=YOUR_DATABASE_URL
```

### 4. 개발 서버 실행

```bash
npm run dev
```

http://localhost:3000 에서 확인하세요.

## 📁 프로젝트 구조

```
dp_protect/
├── app/
│   ├── page.tsx                 # 홈페이지
│   ├── guidelines/page.tsx      # 가이드라인 페이지
│   ├── diagnostic/page.tsx      # 진단 테스트 페이지
│   └── layout.tsx               # 레이아웃
├── lib/
│   ├── firebase.ts              # Firebase 설정
│   └── diagnosticData.ts        # 질문 데이터
├── .env.local                   # 환경 변수
└── public/                      # 정적 파일
```

## 🔧 기술 스택

- **Next.js 16** - React 기반 프레임워크
- **TypeScript** - 타입 안정성
- **Tailwind CSS** - 스타일링
- **Firebase Realtime Database** - 데이터 저장

## 💾 데이터 저장 구조

Firebase Realtime Database에 저장되는 데이터:

```json
{
  "diagnostics": {
    "autoGeneratedId": {
      "name": "사용자이름",
      "timestamp": "2024-01-01T12:00:00.000Z",
      "darkPatternCount": 2,
      "totalQuestions": 5,
      "responses": [
        {
          "questionId": "q1",
          "answer": true,
          "category": "Hidden Options"
        }
      ]
    }
  }
}
```

## 🎨 커스터마이징

### 질문 추가/수정

`lib/diagnosticData.ts` 파일에서 질문 데이터를 수정하세요:

```typescript
export const questions: DiagnosticQuestion[] = [
  {
    id: 'q1',
    image: '/images/q1.png',
    question: '질문 텍스트',
    description: '설명',
    category: '카테고리',
  },
  // 추가 질문...
];
```

### 스타일 수정

모든 페이지에서 Tailwind CSS 클래스를 사용합니다. 색상 스키마를 변경하려면:

```tsx
// 파란색(indigo) → 보라색(purple)으로 변경
className="bg-indigo-600" → className="bg-purple-600"
```

## 📊 Firebase 데이터 조회

Firebase Console에서:
1. Realtime Database 선택
2. `diagnostics` 노드에서 모든 사용자 응답 확인

## 🔒 보안 주의사항

현재 설정은 개발용입니다. 프로덕션 배포 시:

```json
{
  "rules": {
    "diagnostics": {
      ".write": "auth != null",
      ".read": "auth != null"
    }
  }
}
```

## 📝 라이센스

MIT

## 🤝 기여

버그 리포트 및 개선 제안은 환영합니다!

---

**마지막 업데이트**: 2024년 5월
